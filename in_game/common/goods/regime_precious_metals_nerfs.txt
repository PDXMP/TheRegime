# cannot use +- modifiers like country_regime allows 
#INJECT adds a line to the original record
#REPLACE  replaces the record 

REPLACE:goods_gold = {
	method = mining
	category = raw_material
	color = goods_gold
	#default_market_price = 10 #vanilla

	ai_rgo_size_importance = 10 # ai preference to avoid building a city on this rgo
	inflation = yes
	
	ai_rgo_expansion_priority = 0.2
	
	demand_add = {
		nobles = 0.025
	}	
	
	wealth_impact_threshold = {
		all = 1.05
	}

	# Regime Changes
	default_market_price = 7.5 #-25%, nerf
	#transport_cost = 0.5 # -50%, buff
}


REPLACE:silver = {
	method = mining
	category = raw_material
	color = goods_silver
	#default_market_price = 8 #vanilla
	ai_rgo_size_importance = 10 # ai preference to avoid building a city on this rgo
	inflation = yes
	
	ai_rgo_expansion_priority = 0.2
	
	demand_add = {
		nobles = 0.025
	}

	wealth_impact_threshold = {
		all = 1.05
	}


	# Regime Changes
	default_market_price = 6 #-25%, nerf
	#transport_cost = 0.5 # -50%, buff
}
